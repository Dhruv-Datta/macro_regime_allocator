[project]
name = "macro-regime-frontend"
version = "0.1.0"
description = "Streamlit dashboard for the Macro Regime Allocator"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.30",
    "plotly>=5.18",
    "pandas>=1.5",
    "numpy>=1.23",
    "pyyaml>=6.0",
]
